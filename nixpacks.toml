# Nixpacks Configuration for Railway
# Simplified configuration to prevent Nix package conflicts

[phases.setup]
nixPkgs = []

[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node server.js"
